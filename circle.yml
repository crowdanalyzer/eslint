machine:
    node:
        version: 10.14.0
checkout:
    post:
        - mv ./.npmrc-example ./.npmrc
test:
    override:
        - npm run lint
deployment:
    production:
        branch: master
        commands:
            - npm publish
