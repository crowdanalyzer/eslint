machine:
    node:
        version: 6.10.0
checkout:
    post:
        - mv ./.npmrc-example ./.npmrc
test:
    override:
        - npm run lint
deployment:
    production:
        branch: master
        commands:
            - npm publish
